import mainStyles from './../main.module.css';

const Events = () => {
    return (
        <div>
            <p className={mainStyles.title}>Events</p>
        </div>
    );
}

export default Events;